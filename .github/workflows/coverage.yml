name: coverage

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install test tools (repo is markdown-first; not a Python package)
        run: |
          python -m pip install -U pip
          python -m pip install pytest coverage genbadge[all]

      - name: Run tests (if present)
        run: |
          # If you have tests, keep this.
          # If you don't yet, it will fail; see note below.
          coverage run -m pytest -q
          coverage xml

      - name: Generate badge
        run: |
          mkdir -p badges
          genbadge coverage -i coverage.xml -o badges/coverage.svg

      - name: Commit badge (only on default branch)
        if: github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        run: |
          git config user.name "vireon-bot"
          git config user.email "actions@users.noreply.github.com"
          git add badges/coverage.svg
          git commit -m "Update coverage badge" || exit 0
          git push
